---
"talak-web3": patch
"@talak-web3/client": patch
---

Expose `TalakWeb3Client.request()` publicly so apps can call the talak-web3 HTTP API directly instead of only through the typed helper methods.